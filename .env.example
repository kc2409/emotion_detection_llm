# Copy this file to .env and fill in real values. Never commit .env.

# Used for Phase 1/2 conversation generation via the Anthropic API
# (e.g. to script one side of synthetic distressed-user conversations).
ANTHROPIC_API_KEY=

# Used to download Llama-3.2-3B (or swap-in model) from the Hugging Face Hub.
HF_TOKEN=
